<Window x:Class="ProgressDialog.ProgressDialogWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        x:Name="ProgressDialogWin"
             xmlns:local="clr-namespace:ProgressDialog"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    
    <DockPanel LastChildFill="False" VerticalAlignment="Center">
        <Label Content="{Binding Message, Mode=OneWay}" DockPanel.Dock="Top" MinWidth="100"/>
        <Grid DockPanel.Dock="Top">
            <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" MinHeight="20" MinWidth="100" DockPanel.Dock="Top" />
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding ProgressPercent, StringFormat={}{0}%}"/>
        </Grid>
            
        <Button Content="Cancel" Command="{Binding CancelCommand}" DockPanel.Dock="Top" MinWidth="100"/>
    </DockPanel>
</Window>
